import { InnerContainer } from "@/components";

export function Services() {
  return (
    <section className="w-full min-h-96 bg-cover bg-center flex flex-col items-center justify-center object-contain  p-6">
      <InnerContainer>services</InnerContainer>
    </section>
  );
}
